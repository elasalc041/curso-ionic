import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ow-Proyecto2-nostandalone',
  webDir: 'www'
};

export default config;
